[$HOME/.config/vcsh/repo.d/tmux.git]
checkout = vcsh clone https://github.com/rra3/tmux tmux
update   = vcsh run tmux git pull
push     = vcsh run tmux git push
status   = vcsh run tmux git status
gc       = vcsh run tmux git gc
